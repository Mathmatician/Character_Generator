#pragma once

// All abilities
static enum class ABILITIES {
	SECOND_WIND,
	NUM_OF_ABILITIES
};

static enum class FEATS {
	LINGUIST,
	NUM_OF_FEATS
};

static enum class TRAITS {
	DARK_VISION,
	NUM_OF_PROFICIENCIES
};